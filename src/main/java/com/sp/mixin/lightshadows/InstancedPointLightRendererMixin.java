package com.sp.mixin.lightshadows;

// Removed: In Veil 1.21, InstancedPointLightRenderer.setupBufferState() uses VertexArrayBuilder
// instead of raw glEnableVertexAttribArray / glVertexAttribPointer calls.
// The injection targets no longer exist; this mixin must be removed.
